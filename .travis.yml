
matrix:
  include:
    - os: linux
      dist: xenial
      sudo: required

language: go

before_install:
    sudo apt-get update &&
    sudo apt-get dist-upgrade &&
    sudo apt-get install curl wget gnupg make

install:
    make download easy

script:
    make download easy
